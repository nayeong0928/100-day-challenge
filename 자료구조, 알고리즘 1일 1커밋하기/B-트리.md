# B-트리

- 디스크 사용 환경에 적합한 외장 다진 검색 트리 (외장 검색 트리: 디스크가 있는 상태로 검색하는 트리)
- 루트를 제외한 모든 노드는 (k/2)에서 k개의 키를 가짐

## 검색
노드의 여러 키 중에서 검색키와 일치하는 것이 있는지 확인해서 분기할 자식노드를 찾는다

## 삽입

1. x를 삽입할 리프노드 r 찾기
2. r에 공간이 있으면 삽입
3. r에 공간이 없으면 형제노드의 공간 여유 있는지 체크
4. 형제 노드에도 공간이 없으면 노드 2개로 분기

